{
  "name": "biset",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outfile=dist/app.js --target=browser --format=iife --minify-syntax --minify-whitespace --define 'process.env.NODE_ENV=\"production\"' && bun build src/sw.ts --outfile=dist/sw.js --target=browser --format=iife --minify-syntax --minify-whitespace && bun run scripts/inline.mjs",
    "dev": "bun build src/main.ts --outfile=dist/app.js --target=browser --format=iife --watch & bun build src/sw.ts --outfile=dist/sw.js --target=browser --format=iife --watch & bun run scripts/serve.mjs",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.core.json",
    "build:core": "bun build --compile --target=bun-linux-x64 src/core/index.ts --outfile biset-core",
    "core": "bun run src/core/index.ts",
    "test": "for f in test/*.test.ts; do echo \"== $f\"; bun run \"$f\" || exit 1; done",
    "knip": "knip",
    "check": "bun run typecheck && bun run knip && bun run test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/wicg-file-system-access": "^2023.10.7",
    "didcomm-node": "^0.4.1",
    "knip": "^6.27.0",
    "typescript": "^5.9.3",
    "@hpke/core": "1.8.0"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "@noble/post-quantum": "^0.6.1",
    "@scure/base": "^2.2.0",
    "@scure/bip32": "^2.2.0",
    "@scure/bip39": "^2.2.0",
    "bittorrent-dht": "^11.0.12",
    "hash-wasm": "^4.12.0",
    "jmap-jam": "^0.13.1",
    "openpgp": "^6.3.1"
  }
}
