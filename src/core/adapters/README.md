# Transport Adapters

First-party DIDComm, Mail, and ActivityPub adapters will live here. An adapter translates an external protocol into a core ingress/egress request; it cannot query or write a vault directly.
