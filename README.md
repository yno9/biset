# biset

Biset is a communication client that keeps mail and DIDComm data encrypted on the user's own
devices, so no server holds a permanent mailbox or message history.

The current architecture is documented in [ARC.md](ARC.md); the simplification plan and its
open issues are in [PLAN-simplify.md](PLAN-simplify.md).

The previous implementation is retained locally in `src.bak/` and `jmapsmtp.bak/`.

> Placeholder: installation, deployment, and user documentation will be added with the first working milestone.
